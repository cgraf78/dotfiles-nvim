# Neovim capability tests

These helpers exercise only editor-owned configuration, launchers, merge hooks,
and doctor checks. Development plugins and toolchains are tested by the
`dotfiles-dev` overlay; cross-overlay composition remains a top-level
`dotfiles` responsibility.
