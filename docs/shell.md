# Editor shell integration

The editor overlay contributes small `.d` fragments: default editor variables,
the `vi` alias, and selection of the editor-specific ripgrep configuration.
General shell policy remains in the top-level `dotfiles` repository.
