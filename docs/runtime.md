# Editor runtime

The overlay owns its Neovim merge hook, component doctor extension, and focused
test suites. Shared Dot extension APIs and compatibility helpers are inherited
from the always-active top-level `dotfiles` repository.
