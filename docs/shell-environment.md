# Editor environment

`60-editor.sh` selects the ripgrep configuration that emits Neovim-aware
hyperlinks. `80-editor-defaults.sh` selects `nvim` and the default colorscheme.
The fragments rely only on the lower `base` profile and this overlay.
